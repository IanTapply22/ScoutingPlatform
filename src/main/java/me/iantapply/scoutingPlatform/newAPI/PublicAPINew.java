package me.iantapply.scoutingPlatform.newAPI;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class PublicAPINew {

    /*
    private final ConfigurationUtils configurationUtils;
    public PublicAPI(ConfigurationUtils configurationUtils) {
        this.configurationUtils = configurationUtils;
    }

    @RequestMapping(method = RequestMethod.GET, value = "scoutingData", produces = MediaType.APPLICATION_JSON_VALUE)
    public ScoutingData allScoutingData() throws ExecutionException, InterruptedException {

        // Return stats from the specified year
        return FirebaseService.getTeamStatsInformation(teamID, year);
    }
     */
}
